services:
  calendar:
    build: .
    ports:
      - "3000:3000"
    environment:
      - DATABASE_URL=/app/data/calendar.db
    volumes:
      - calendar-data:/app/data
    restart: unless-stopped

volumes:
  calendar-data:
